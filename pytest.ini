[pytest]
addopts = -n 4 --dist loadgroup
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::PendingDeprecationWarning:starlette.formparsers
    ignore::DeprecationWarning:jose.jwt
markers =
    postgresql: requires TEST_DATABASE_URL and validates the PostgreSQL migration path
